# Force LF on files that must have Unix endings when deployed to Linux
*.sh        text eol=lf
*.service   text eol=lf
*.conf      text eol=lf
*.yml       text eol=lf

# Everything else: use platform-native line endings
*           text=auto
